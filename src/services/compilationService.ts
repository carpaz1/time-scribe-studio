
// Re-export the main compilation service for backward compatibility
export { CompilationService } from './compilation';

// Re-export types that might be used externally
export type { CompilationResult, ProgressCallback } from './compilation/types';
